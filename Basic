import PyAutoGUI
import random
import time

i=0
while i<30:
	#(X,Y) position of the like button on desktop mode of tinder. Get it by using python.position()
		PyAutoGUI.click(926,597)
		time.sleep(random.randint(0,4))
		i+=1
